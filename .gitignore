.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Generated islanding schedules and cached runs
data/*.json
!data/.gitkeep

*.log
.DS_Store

# Lab allocation — internal platform state (addresses, hostname, secret paths).
# Regenerated per-deployment by the platform's tenant tooling; not public.
LAB.md

# Vendored at build time from the installed plotly package (see Dockerfile).
# 4.6 MB of third-party minified JS does not belong in source history.
static/plotly.min.js

# mkdocs build output, and the figures/notes the site build generates.
site/
site_src/figures/
site_src/notes/
