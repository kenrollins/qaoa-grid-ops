# qaoa-grid-ops — lab allocation

Generated by `/data/code/dmz/tools/new-tenant`. This file records only THIS
app's allocation. The platform contract (networking, secrets, inference, DNS,
telemetry rules) lives in **`/data/code/dmz/ONBOARDING.md`** — read it there,
don't copy it here.

| | |
|---|---|
| VLAN 13 address | `10.0.13.103` (public face on the `dmz13` macvlan) |
| Hostname | https://qaoa-grid-ops.lab.kenrollins.dev (passkey-protected) |
| Secrets | `/data/docker/qaoa-grid-ops/.env` (600, never commit) |
| Inference | gateway `http://10.0.13.201:4000/v1`, key `LITELLM_API_KEY`, models: nim/llama-3.1-8b |
| Shared Postgres | `10.0.13.220:5432` (be a client; don't stand up a rival) |

## Compose shape

```yaml
networks:
  qaoagridops_net: {driver: bridge}   # private backends — no VLAN address
  dmz13: {external: true}

services:
  app:
    env_file: /data/docker/qaoa-grid-ops/.env
    networks:
      qaoagridops_net: {}
      dmz13: {ipv4_address: 10.0.13.103}    # needed for DMZ isolation, not just reachability
```

## Remaining manual step
Operator must add the internal Unbound override `qaoa-grid-ops.lab.kenrollins.dev` → `10.0.13.3`
(**specific host only — never a wildcard**, OPNsense #8051).
